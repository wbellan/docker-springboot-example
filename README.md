# Docker, SpringBoot, etc...

A learning project for Dockerizing SpringBoot applications, along with other things....

## Installation

1. mvn clean package docker:build
2. docker-compose up -d

## Usage

http://localhost:3000/
